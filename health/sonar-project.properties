# Required metadata
sonar.projectKey=galeb-health
sonar.projectName=galeb-health
sonar.projectVersion=4.0.10-SNAPSHOT

# Comma-separated paths to directories with sources (required)
sonar.sources=core/src

# Language
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8
